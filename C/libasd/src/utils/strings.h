#ifndef __STRINGS_H__
#define __STRINGS_H__

#include "../standard_libraries.h"

/**
 * @brief      Copies a string from a buffer.
 *
 * @param[in]  buffer  The buffer.
 *
 * @return     The pointer to the string.
 */
char* new_string(char* buffer);

#endif