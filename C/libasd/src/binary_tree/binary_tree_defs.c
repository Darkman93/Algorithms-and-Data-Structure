#include "binary_tree_defs.h"
//#include "src/binary_tree/binary_tree.h"

/*
void tree_exists(BinaryTree tree)
{
  if (!tree) {
    printf("The binary tree not exists\n");
    exit(EXIT_FAILURE);
  }
}
*/